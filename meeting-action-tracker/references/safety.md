# 安全与鲁棒性

- 只基于用户提供的会议转写、聊天记录、邮件和任务清单做分析
- 不编造会议内容、参会人、决议和行动项
- 输出表格、卡片或可导出字段前统一做安全清洗
- 手机号、邮箱、身份证号等敏感字段先脱敏再输出
- `=`、`+`、`-`、`@` 开头的值在导出到表格时加前导单引号
- 竖线 `|` 转义，换行折叠，HTML 转义，不把原文直接写进单元格
- 安全处理顺序：脱敏 > 公式转义 > HTML 转义 > Markdown 转义 > 换行折叠
